name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  sonarqube:
    name: SonarCloud Scan
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for better analysis

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Run tests with coverage
        continue-on-error: true
        run: |
          pytest tests/ --cov=src --cov=main --cov-report=xml --cov-report=term -v
      
      - name: Verify coverage file exists
        run: |
          if [ -f coverage.xml ]; then
            echo "✓ coverage.xml generated successfully"
            ls -lh coverage.xml
          else
            echo "⚠ coverage.xml not found, creating empty one"
            echo '<?xml version="1.0" ?><coverage version="1.0"><packages/></coverage>' > coverage.xml
          fi

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        continue-on-error: true
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
